$(document).ready  

    (
        function()

{
        var btmobile = $('.bt-menu');
                  $(btmobile).on('click', function()
        {
            $('.menu-container ul').toggleClass('abrir-menu');
 });
   
});